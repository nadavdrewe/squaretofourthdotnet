@model IEnumerable<Revel._808nd.com.Classes.MiscSettings>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LateOpeningStoreNotifier)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LateOpeningStoreMinutesWindow)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LateOpeningStoreNotifier)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LateOpeningStoreMinutesWindow)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
